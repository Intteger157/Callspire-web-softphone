# Dependencies
**/node_modules/

# Build output (SPA)
softphone-web/dist/

# Local env / secrets
.env
.env.*
!.env.example

# Gateway runtime config belongs in callspire-pbx-gateway (config.example.yaml), not here
config.yaml

# OS / IDE
.DS_Store
Thumbs.db
